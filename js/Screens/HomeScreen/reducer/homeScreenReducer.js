'Use Strict';

const initialState = {
    
};

const HomeScreenReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default HomeScreenReducer;
